# ADR-0001: Revenue is computed in the warehouse

## Status

Accepted.

## Decision

All financial aggregation happens in SQL against the `orders` table, not in
application code. `app/service.py` only orchestrates; see `REVENUE_QUERY`.

## Consequences

Revenue changes require a migration on `stg_orders`, keeping one source of truth.
